# elvui-updater

(Say no to extra clients.)

## Usage

On Windows, e.g.

```sh
python update_elvui.py -w "D:\World of Warcraft\"
```

Place it in a .bat file and use Task Scheduler to automate daily.
